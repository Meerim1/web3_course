# Задания по Solidity

## Часть 1. Контракт с владельцем и модификатором доступа

### Требования:

1. Создай контракт.
2. Добавь переменную:
   ```solidity
   address public owner;
   ```
3. Инициализируй `owner` адресом создателя контракта.
4. Реализуй модификатор:
   ```solidity
   modifier onlyOwner
   ```
   который разрешает вызов функции только владельцу.
5. Добавь функции:
   - `changeOwner(address newOwner)`
   - `restrictedAction()`

### Условия:
- Обе функции обязаны использовать модификатор `onlyOwner`
- `restrictedAction()` просто возвращает строку `"restricted action"`
📌 **Зачем:** понять, как ограничивать доступ к функциям.

---

## Часть 2. Mapping и защита от повторного действия (голосование)
### Требования:
1. Создай mapping:
   ```solidity
   mapping(address => bool) public hasVoted;
   ```
2. Реализуй функцию:
   ```solidity
   function vote()
   ```
   без аргументов.
### Логика функции `vote()`:
- пользователь может проголосовать только один раз
- если пользователь уже голосовал — транзакция должна отклоняться через `require`
- при успешном голосовании:
  ```solidity
  hasVoted[msg.sender] = true;
  ```
3. Добавь функцию:
   ```solidity
   function getVotesCount() public view returns (uint256)
   ```
   которая возвращает общее количество голосов.
📌 **Зачем:** защита от повторных действий и работа с mapping.

---

## Часть 3. Mapping и логика баланса пользователя
### Требования:
1. Создай mapping:
   ```solidity
   mapping(address => uint256) public balances;
   ```
2. Реализуй функции:
   - `deposit(uint256 amount)`
   - `withdraw(uint256 amount)`
### Логика:
- `deposit` увеличивает баланс вызывающего адреса
- `withdraw` уменьшает баланс
- Нельзя вывести больше, чем есть:
  ```solidity
  require(balances[msg.sender] >= amount)
  ```
- Пользователь может выводить только свой баланс
- Вывод должен отправлять ETH пользователю
📌 **Зачем:** учёт средств пользователей и безопасность.

---

## Часть 4. Mapping с флагами доступа (Whitelist)
### Требования:
1. Создай mapping:
   ```solidity
   mapping(address => bool) public whitelist;
   ```
2. Реализуй функции:
   - `addToWhitelist(address user)`
   - `removeFromWhitelist(address user)`
   - `isWhitelisted(address user) → bool`
### Условия:
- Добавлять и удалять адреса может только владелец
- Используй `require` или `onlyOwner`
3. Добавь функцию:
   ```solidity
   restrictedAction()
   ```
   которая возвращает строку `"restrictedAction"`
   Эта функция должна быть доступна только адресам из whitelist
📌 **Зачем:** управление правами доступа.

---

## Часть 5. Итоговое задание — Мини-банк с владельцем
### Требования:
Создай контракт `MiniBank` со следующей логикой:
1. Владелец контракта задаётся в конструкторе
2. Любой пользователь может:
   - отправить ETH в контракт (через `payable`)
3. Только владелец может:
   - вывести весь баланс контракта
4. Добавь событие:
   ```solidity
   event Withdraw(address to, uint256 amount);
   ```
5. При выводе средств:
   - отправь ETH владельцу
   - сэмить событие `Withdraw`
📌 **Зачем:** объединить всё - owner, ETH, события, безопасность.
